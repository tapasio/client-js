describe('TapasQuery', () => {
  describe('constructor', () => {
    it('should accept the main tapas object as argument and an Object', () => {
      // ??
    })
  })

  describe('fetch', () => {
    it('method should be exposed', () => {
      
    })

    it('should not accept any argument', () => {
      
    })

    it('should return a TapasResponse', () => {
      
    })
  })

  describe('subscribe', () => {
    it('method should be exposed', () => {
      
    })

    it('should not accept any argument', () => {
      
    })

    it('should return a TapasResponse', () => {
      
    })
  })

  describe('filter', () => {
    it('method should be exposed', () => {
      
    })

    it('should accept an Array as argument', () => {
      
    })

    it('should return a TapasQuery', (done) => {
      
    })
  })
})