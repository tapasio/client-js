describe('TapasResponse', () => {
  describe('constructor', () => {
    it('should extend Rx.Observable', () => {
      
    })
  })

  describe('subscribe', () => {
    it('method should be exposed', () => {
      
    })

    it('should call Rx.Observable.subscribe', () => {
      
    })
  })

  describe('then', () => {
    it('method should be exposed', () => {
      
    })

    it('should call Rx.Observable.subscribe then unsubscribe after the first response', () => {
      
    })
  })
})